title: "Your Name"
description: "Personal site"

# If this is a project page (https://username.github.io/repo), set baseurl: "/repo"
# If this is a user/org page (https://username.github.io), keep baseurl: ""
baseurl: ""
url: ""

markdown: kramdown

permalink: pretty

defaults:
  - scope:
      path: ""
    values:
      layout: default

exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
